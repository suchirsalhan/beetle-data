# =====================================================
# Core Data Processing & Streaming
# =====================================================
datasets>=2.18.0
pyarrow>=14.0.0
numpy>=1.24
pandas>=2.0
tqdm>=4.65
ftfy>=6.1


# Language detection
langdetect>=1.0.9
langid>=1.1.6
 
# ML / embeddings (for trained regressor)
scikit-learn>=1.3
sentence-transformers>=2.2   # for embeddings (optional but recommended)
 

# =====================================================
# Tokenization & Model Handling
# =====================================================
transformers>=4.40.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
protobuf>=4.25
tiktoken>=0.6.0

# =====================================================
# Hugging Face Hub & Utilities
# =====================================================
huggingface_hub>=0.23.0

# =====================================================
# Performance / Training / Multi-GPU
# =====================================================
accelerate>=0.30.0

# Optional but recommended for large-scale streaming / training
aiohttp>=3.9.0          # async dataset streaming backend
fsspec>=2024.2.0        # filesystem abstraction (used by datasets)
psutil>=5.9.0           # memory monitoring
tqdm>=4.66.0            # progress bars

# =====================================================
# Optional (only if scaling further)
# =====================================================
# deepspeed>=0.14.0      # for model training (not needed for tokenizer)
# wandb>=0.16.0          # experiment tracking


# Deep learning (optional — for LLM annotation)
# torch>=2.0
# transformers>=4.35
